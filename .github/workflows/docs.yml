# .github/workflows/docs.yml

name: CDQN Documentation

on:
  push:
    branches:
      - main

jobs:
  deploy_docs:
    name: Deploy Docs to GitHub Pages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Build Rust Documentation
        run: cargo doc --no-deps --all-features

      # --- NEW STEP ADDED HERE ---
      - name: Copy Markdown Documentation
        run: |
          # Create a directory within the generated docs to hold our .md files
          mkdir -p ./target/doc/docs
          # Copy the contents of the Docs directory
          cp -r Docs/*.md ./target/doc/docs/
          # Copy the root README.md to the doc root
          cp README.md ./target/doc/

      - name: Create Redirect Page
        run: echo '<meta http-equiv="refresh" content="0; url=cdqn/index.html">' > ./target/doc/index.html

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./target/doc
