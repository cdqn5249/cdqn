# .github/workflows/docs.yml

name: CDQN Documentation

on:
  push:
    branches:
      - main

jobs:
  deploy_docs:
    name: Deploy Docs to GitHub Pages
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Build Rust Documentation
        run: cargo doc --no-deps --all-features

      # --- THIS IS THE NEW, ROBUST SOLUTION ---
      # It replaces the old "Copy" and "Redirect" steps.
      - name: Build and Organize Final Documentation Site
        run: |
          # 1. Copy all markdown files from the Docs directory into the doc root.
          cp Docs/*.md ./target/doc/
          # 2. Copy the main README to the doc root as well.
          cp README.md ./target/doc/
          
          # 3. Create a new, clean index.html landing page.
          echo '<!DOCTYPE html>
          <html>
          <head>
            <title>CDQN Project Documentation</title>
            <meta charset="utf-8">
            <style>
              body { font-family: sans-serif; margin: 2em; }
              h1, h2 { color: #333; }
              ul { list-style-type: none; padding: 0; }
              li { margin: 0.5em 0; }
              a { text-decoration: none; color: #0066cc; }
              a:hover { text-decoration: underline; }
              .section { border-top: 1px solid #ccc; padding-top: 1em; margin-top: 2em; }
            </style>
          </head>
          <body>
            <h1>CDQN Project Documentation</h1>
            
            <div class="section">
              <h2>Rust API Documentation</h2>
              <ul>
                <li><a href="cdqn/index.html">API Reference (cargo doc)</a></li>
              </ul>
            </div>

            <div class="section">
              <h2>Design & Planning Documents</h2>
              <ul>' > ./target/doc/index.html

          # 4. Dynamically add a link for each markdown file to the index.html.
          for file in ./target/doc/*.md; do
            if [ -f "$file" ]; then
              filename=$(basename "$file")
              echo "                <li><a href=\"$filename\">$filename</a></li>" >> ./target/doc/index.html
            fi
          done

          # 5. Close the HTML tags.
          echo '              </ul>
            </div>
          </body>
          </html>' >> ./target/doc/index.html

      # This debugging step is kept to ensure everything is in the right place.
      - name: List generated files for debugging
        run: ls -R ./target/doc

      - name: Deploy to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./target/doc
