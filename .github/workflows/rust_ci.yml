# Conceptual File Path: .github/workflows/rust_ci.yml
# Vibe coding engine : Gemini Flash-Lite Latest, Google

name: Chronos Rust CI

on:
  push:
    branches:
      - main # Trigger on push to the main branch
    paths:
      - '/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
  pull_request:
    branches:
      - main
    paths:
      - '/**'
      - 'Cargo.toml'
      - 'Cargo.lock'

jobs:
  chronos_ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          profile: minimal
          override: true

      - name: Install Rustfmt
        uses: actions-rs/cargo@v1
        with:
          command: install
          args: --locked rustfmt

      - name: Check Code Formatting (rustfmt)
        uses: actions-rs/cargo@v1
        with:
          command: run
          args: --fmt --check # Checks if code is formatted correctly

      - name: Build Chronos Engine
        uses: actions-rs/cargo@v1
        with:
          command: build
          args: --verbose

      - name: Run Unit Tests
        uses: actions-rs/cargo@v1
        with:
          command: test
          args: --verbose

# BaDaaS License Attribution
# This workflow file is part of the Chronos AI project, licensed under BaDaaS.
# Generated by the vibe coding engine.
