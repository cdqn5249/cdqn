# cdqn/Cargo.toml
# File under the BaDaaS license, code generated by ai assistant: Gemini 2.5 Pro, Google

[workspace]
resolver = "2"
members = [
    "crates/common",
    "crates/cryptocore",
    "crates/hlc",
    "crates/cdu",
]

[workspace.package]
authors = ["Christophe Duy Quang Nguyen <contact@christophe-nguyen.com>"]
edition = "2021"
license = "BaDaaS"
version = "1.0.0"

# --- THE CRITICAL FIX ---
# The [workspace.dependencies] table is being removed completely.
# While useful for large projects, it is the source of our current ambiguity.
# Each crate will now define its dependencies explicitly, which is more robust
# for a project of this size.
[workspace.dependencies]
# This section is now intentionally left empty.```
