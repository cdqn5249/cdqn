# BaDaaS License: This file is governed by the BaDaaS license.
# File Path: /Cargo.toml

# This file defines the entire CDQN project as a Rust Workspace.

[workspace]

# This section lists all the individual crates that are part of our project.
members = [
    "src/kernel/crypto",
    # As we create more modules (e.g., K.HLC), we will add their paths here.
]

# This explicitly sets the dependency resolver to the modern, recommended version,
# resolving the warning from the CI build.
resolver = "2"
