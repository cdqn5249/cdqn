# This file is under license BaDaaS, generated by the vibe coding engine Gemini Flash-Lite Latest from Google.
# Cargo.toml

[package]
name = "cdqn"
version = "0.1.0"
edition = "2021"

[dependencies]
# Minimal dependency for generating random numbers for the ID's entropy component.
rand = "0.8" 
