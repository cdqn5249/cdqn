# cdqn/crates/cryptocore/Cargo.toml
# File under the BaDaaS license, code generated by ai assistant: Gemini 2.5 Pro, Google

[package]
name = "cryptocore"
description = "Core cryptographic functionalities for the CDQN ecosystem."
# Inherits version, authors, edition, license from the workspace root Cargo.toml

[dependencies]
# --- THE CRITICAL FIX ---
# By adding `workspace = true`, we are explicitly telling Cargo to look for the
# version and details of these dependencies in the [workspace.dependencies] table
# of the root `Cargo.toml`. This is the missing link that resolves the errors.
ed25519-dalek = { workspace = true }
sha3 = { workspace = true }
rand = { workspace = true }
