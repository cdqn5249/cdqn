# cdqn/crates/cdu/Cargo.toml
# File under the BaDaaS license, code generated by ai assistant: Gemini 2.5 Pro, Google

[package]
name = "cdu"
description = "The Causal Data Unit (CDU) implementation, the core data structure of the CDQN ecosystem."
# Inherits version, authors, edition, license from the workspace root Cargo.toml

[dependencies]
# --- THE CRITICAL FIX ---
# We now depend on our local crates via a direct path instead of workspace inheritance.
# This removes all ambiguity and resolves the dependency resolution error.
cryptocore = { path = "../cryptocore" }
hlc = { path = "../hlc" }
common = { path = "../common" }
