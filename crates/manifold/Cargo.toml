# crates/manifold/Cargo.toml
# File under the BaDaaS license, code generated by the ai assistant: Gemini Flash Latest, Google

[package]
name = "cdqn-manifold"
version = "0.1.0"
edition = "2021"
description = "Emergent in-memory knowledge graph projection from CDUs, with causal indexing and verification"
license = "BaDaaS"
publish = false

[dependencies]
cdqn-cdu = { path = "../cdu" }
cdqn-cryptocore = { path = "../cryptocore" }
serde_json = "1.0" # <-- FIX: Added serde_json for persistence logic

[dev-dependencies]
tempfile = "3.10" # <-- FIX: Added tempfile for tests
