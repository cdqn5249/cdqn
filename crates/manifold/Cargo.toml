# crates/manifold/Cargo.toml
# File under the BaDaaS license, code generated by the ai assistant: Qwen3-Max, Alibaba

[package]
name = "cdqn-manifold"
version = "0.1.0"
edition = "2021"
description = "Emergent in-memory knowledge graph projection from CDUs, with causal indexing and verification"
license = "BaDaaS"
publish = false

[dependencies]
cdqn-cdu = { path = "../cdu" }
cdqn-cryptocore = { path = "../cryptocore" }
