#path : cdqn/chronos_ai/Cargo.toml
[dependencies]
# For CDU Hashing (Content-Addressing)
sha2 = "0.10" 
hex = "0.4"
# For Deterministic Serialization (Required for Hashing Complex Structs)
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
